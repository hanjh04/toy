export default {
    fetchedBookList(state) {
        return state.bookList;
    },
    fetchedBookDetail(state) {
        return state.bookDetail;
    },
    fetchedImage(state) {
        return state.image;
    }
}